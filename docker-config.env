# WordPress Testing Environment Configuration
# Customize these values as needed for your local setup

# WordPress Configuration
WORDPRESS_PORT=8080
WORDPRESS_DEBUG=1

# Database Configuration  
MYSQL_DATABASE=wordpress
MYSQL_USER=wordpress
MYSQL_PASSWORD=wordpress
MYSQL_ROOT_PASSWORD=rootpassword

# phpMyAdmin Configuration
PHPMYADMIN_PORT=8081

# Notes:
# - Change WORDPRESS_PORT if 8080 is already in use
# - Change PHPMYADMIN_PORT if 8081 is already in use
# - Set WORDPRESS_DEBUG=0 to disable debug mode
# - Database credentials can be customized for security 